#tuples are just like lists
# these immutable and not dynamic
# only a handful of methods work on them and those too do not change the data of the tuple
my_tuple = ('vinod','uma','sagar','bennett')
print(my_tuple)
minimum = min(my_tuple)
print(minimum)
max = max(my_tuple)
print(max)

print(my_tuple[2])
